MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=adaptive_diagnostic

# Optional (for AI answer evaluation + study plan generation)
# Create your own key and paste it here. Never commit real keys.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

